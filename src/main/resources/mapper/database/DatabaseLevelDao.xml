<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcidea.erms.dao.database.DatabaseLevelDao">

    <select id="findSonCountListBySidDid" resultType="java.lang.Integer">
        select count(d_id)
        from database_level
        where s_id = #{sid}
        and total_did = #{did}
        and total_flag = 0
    </select>

    <select id="findSonListBySidTotalDid" resultType="java.lang.Long">
        select d_id
        from database_level
        where s_id = #{sid}
        and total_did = #{totalDid}
        and total_flag = 0
    </select>

    <select id="findDidTotalIdListBySid" resultType="com.kcidea.erms.model.database.DidTotalIdModel">
        select d_id,total_did
        from database_level
        where s_id = #{sid}
        and total_flag = 0
    </select>

    <select id="findDidSetBySid" resultType="java.lang.Long">
        select d_id
        from database_level
        where s_id = #{sid}
    </select>

    <select id="findLevelTotalDatabaseSelectList" resultType="java.lang.Long">
        select distinct d_id
        from database_level
        where s_id = #{sid} and total_flag = 1
    </select>

    <select id="findListBySid" resultType="com.kcidea.erms.domain.database.DatabaseLevel">
        select *
        from database_level
        where s_id = #{sid}
    </select>

    <select id="findOneBySidDid" resultType="com.kcidea.erms.domain.database.DatabaseLevel">
        select *
        from database_level
        where s_id = #{sid}
        and d_id = #{did}
    </select>

    <delete id="deleteBySidDid">
        delete
        from  database_level
        where s_id = #{sid}
        and (d_id = #{did} or total_did = #{did})
    </delete>

</mapper>