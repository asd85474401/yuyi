<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcidea.erms.dao.database.DatabaseAttachmentDao">

    <select id="findListBySidUniqueIdType" resultType="com.kcidea.erms.domain.database.DatabaseAttachment">
      select *
      from database_attachment
      where s_id =#{sid} and unique_id = #{id} and type = #{type}
    </select>
    <select id="findNamePathListBySidUniqueIdType" resultType="com.kcidea.erms.model.database.AttachmentModel">
      select file_name,file_path
      from database_attachment
      where s_id =#{sid} and unique_id = #{id} and type = #{type}
    </select>
    <delete id="deleteBySidUniqueIdType">
        delete from database_attachment
        where s_id =#{sid} and unique_id = #{id} and type = #{type}
    </delete>

</mapper>