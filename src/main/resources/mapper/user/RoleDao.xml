<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcidea.erms.dao.user.RoleDao">
    <delete id="deleteByIdSid">
        DELETE FROM role
        WHERE s_id = #{sid} and id = #{id}
    </delete>
	<select id="findPageListByName" resultType="com.kcidea.erms.model.user.RoleInfoModel">
        SELECT r.id,r.name,r.remark,count(u.id) as userCount
        FROM role r
        LEFT JOIN `user` u on u.role_id = r.id and u.s_id = r.s_id
        WHERE r.s_id = #{sid}
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
        GROUP BY r.id
    </select>
    <select id="findOneBySidName" resultType="com.kcidea.erms.domain.user.Role">
        SELECT * FROM role
        WHERE s_id = #{sid} and name = #{name}
    </select>
 	<select id="findNameById" resultType="java.lang.String">
        select name from role where id = #{id}
    </select>
    <select id="findIdNameListBySid" resultType="com.kcidea.erms.model.common.IdNameModel">
        select id,name from role where s_id = #{sid}
    </select>
</mapper>
