<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcidea.erms.dao.ers.CompanyDao">

    <select id="findListBySid" resultType="com.kcidea.erms.model.common.IdNameModel">
        select id,name from company where s_id = #{sid} or s_id = 0
    </select>
    <select id="findBySidName" resultType="com.kcidea.erms.domain.ers.Company">
        select id, s_id, type, name
        from company
        where (s_id = #{sid} or s_id = 0) and name = #{name}
    </select>
</mapper>
