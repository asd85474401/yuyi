<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcidea.erms.dao.ers.SchoolDatabaseAliasDao">

    <select id="findOneBySidName" resultType="com.kcidea.erms.domain.ers.SchoolDatabaseAlias">
        select id, s_id, d_id, d_name, url
        from school_database_alias
        where s_id = #{sid} and d_name = #{name}
        limit 1
    </select>
    <select id="findOneBySidDid" resultType="com.kcidea.erms.domain.ers.SchoolDatabaseAlias">
        select id, s_id, d_id, d_name, url
        from school_database_alias
        where s_id = #{sid} and d_id = #{did}
        limit 1
    </select>
    <select id="findIdNameListBySid" resultType="com.kcidea.erms.model.common.IdNameModel">
        select d_id id, d_name name
        from school_database_alias
        where s_id = #{sid}
    </select>
</mapper>
