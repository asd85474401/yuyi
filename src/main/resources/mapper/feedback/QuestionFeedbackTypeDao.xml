<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcidea.erms.dao.feedback.QuestionFeedbackTypeDao">
    <select id="findNameBySidTypeId" resultType="java.lang.String">
        select name
        from question_feedback_type
        where s_id = #{sid} and id = #{typeId}
    </select>
    <select id="findTypeIdBySidName" resultType="java.lang.Long">
        select id
        from question_feedback_type
        where s_id = #{sid} and name = #{typeName}
    </select>
    <select id="findIdNameListBySid" resultType="com.kcidea.erms.model.common.IdNameModel">
        select id,name
        from question_feedback_type
        where s_id = #{sid}
    </select>
</mapper>